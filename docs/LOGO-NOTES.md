# White logo treatment

Latest revision: removed the diagonal sparkle/glare from the upper-right arc using the built-in image editor. The website, self-contained design preview and source archive now use this smooth-arc version.

Revision prompt: “Remove the star sparkle / diagonal glare spike on the upper-right curved swoosh. That arc must be a single smooth continuous curved white band with a clean uninterrupted outer contour, no crossing strokes, no star, no glint and no shine. Preserve APB lettering, outline gaps, DETAILING, spacing and proportions. Output transparent PNG.”

The original supplied `public/logo.png` is retained. The website now uses `public/logo-white.png`, a white-on-transparent derivative created with the built-in image-editing tool. The header uses the actual logo rather than a thumbnail plus typed duplicate. The hero mark sits directly on navy, with the blue rectangular panel removed. The footer and owner workspace use the same asset.

The PNG has a real alpha channel. Transparent background and transparent lettering counters were checked from its pixel data. This is a raster derivative, not a vector master or a pixel-identical tracing. The SPL artwork was not supplied in this task and was not used as a visual source.

Final edit prompt:

> Edit supplied APB DETAILING logo into FLAT WHITE INK on TRUE TRANSPARENT ALPHA. It must be a usable PNG logo, NOT an illuminated sign or artwork. STRICT: absolutely NO GLOW, NO BLUR, NO LIGHTING, NO HALO, NO SHADOW, NO GRADIENT, NO black/grey background, NO coloured pixels. Transparent pixels everywhere outside the white logo and within lettering holes and outline gaps. Preserve the exact original APB shapes, original inner outlines, the original spaced DETAILING word and original swoosh. White ink shapes have crisp hard edges. Keep all three letters APB clearly legible with transparent counters. Preserve original relative proportions. Tightly framed horizontal logo with small clear transparent margin. White #FFFFFF only with transparent alpha background. Text exactly APB and DETAILING. This is a simple flat graphic colour conversion and background removal, not a redesign.

The static design preview was regenerated after integration. Production build and live deployment remain pending the previously documented account and environment setup.
